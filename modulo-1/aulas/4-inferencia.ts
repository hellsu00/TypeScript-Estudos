{
    const nome = 'Felipe'
    const idade = 29
    const programmer = true

    function ola(nome: string): void {
        console.log(`olá ${nome}`)
    }
    let arr = [1, 'Felipe', true, null]

    const numeros = [1,2,3,4,5,6]
    const soma = numeros.reduce((acc, num) => acc + num)
}